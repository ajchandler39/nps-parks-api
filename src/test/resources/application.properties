# Test config: in-memory H2 + dummy NPS settings (the data loader is mocked out in tests).
spring.datasource.url=jdbc:h2:mem:npstest;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

nps.api.base-url=http://localhost
nps.api.key=test
